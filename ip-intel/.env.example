# Optional free-tier enrichment. ip-intel runs fully without any of these.
# Copy to .env (gitignored) and `export $(cat .env | xargs)` or use your own loader.

# AbuseIPDB free tier — https://www.abuseipdb.com/register  (1000 checks/day free)
# Adds a confidence score + recent report categories to the Abuse section.
ABUSEIPDB_KEY=
